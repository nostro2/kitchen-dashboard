{
  "name": "kitchen-dashboard",
  "version": "1.0.0",
  "description": "Household chores dashboard",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && python3 server.py 3000",
    "start": "npm run build && python3 server.py 3000"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
